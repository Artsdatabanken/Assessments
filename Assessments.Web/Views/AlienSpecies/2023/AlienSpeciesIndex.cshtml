@using Assessments.Web.Models.AlienSpecies
@using Assessments.Web.Views.AlienSpecies._2023

@inject IStringLocalizer<AlienSpeciesIndex> Localizer

@model AlienSpeciesListViewModel

@{
    Layout = "~/Views/AlienSpecies/_Layout.cshtml";
    ViewBag.Title = Localizer["list_title_year"];

    var breadcrumbs = new Dictionary<string, string>
    {
        { "Arter", "https://artsdatabanken.no/arter" },
        { ViewBag.Title, string.Empty }
    };

    var citationViewModel = new CitationForListViewModel
    {
        CitationString = Constants.AlienSpecies2023CitationString
    };

    var pageHeaderViewModel = new PageHeaderViewModel
    {
        HeaderText = Localizer["list_title_year"],
        HeaderByline = Constants.AlienSpecies2023HeaderByline
    };

    var introductionViewModel = new IntroductionViewModel
    {
        Introduction = Localizer["intro"]
    };
}

@section Breadcrumb {

    <partial name="_Breadcrumb" model="breadcrumbs" />
}

<div class="content-placer bigmid">
    <div class="mid-content">
        <div class="title_with_image">
            <div class="title_with_header">
                <partial name="/Views/Shared/_PageHeader.cshtml" model="@pageHeaderViewModel" />
            </div>
            <div class="title_with_ingress">
                <partial name="/Views/Shared/_Introduction.cshtml" model="@introductionViewModel" />
            </div>
        </div>
        <form id="search_and_filter_form" aps-action="" method="get">
            <partial name="/Views/AlienSpecies/2023/ListPartials/_Search.cshtml" />
            <partial name="/Views/AlienSpecies/2023/ListPartials/_Filters.cshtml" />
        </form>
        <partial name="/Views/Shared/_CitationForList.cshtml" model="citationViewModel"/>
    </div>
</div>